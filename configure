#!/bin/bash

app=$(snapctl get app)
if [ -z "${app}" ]; then
    # This could be the first time the snap is configured (at install time)
    # let's choose a default instead of exiting
    app=photoviewer
fi

# TODO: reject invalid app values
echo "app=$app" > $SNAP_DATA/.config


